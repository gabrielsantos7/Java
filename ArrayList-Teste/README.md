
# ArrayList-Teste

Projeto de Teste de Funcionalidades do ArrayList

Este projeto consiste em um programa desenvolvido em Java que testa diversas funcionalidades do `ArrayList`. O programa é voltado para a manipulação de objetos do tipo `Aluno`, onde os alunos são armazenados em um `ArrayList` e diversas operações são realizadas sobre essa estrutura de dados.

## 📗Classe Aluno

A classe `Aluno` é a entidade principal do projeto. Ela representa um aluno e contém atributos como nome, matrícula e demais informações pertinentes. A classe `Aluno` é utilizada para criar objetos que serão posteriormente armazenados no `ArrayList`.

## 📘Gerenciador de Operações

O projeto também inclui uma classe chamada `GerenciadorAluno`, responsável por realizar diferentes operações no `ArrayList` que armazena os objetos do tipo `Aluno`. Essas operações incluem salvar um novo aluno com os parâmetros recebidos do usuário, pesquisar alunos pelo nome, remover alunos pela matrícula e listar todos os alunos cadastrados.

O `GerenciadorAluno` oferece métodos que facilitam a interação com o `ArrayList`. Ele utiliza métodos disponíveis na classe `ArrayList` para adicionar, pesquisar, listar e remover elementos, utilizando as funcionalidades nativas da estrutura de dados.

## 📕Objetivos do Projeto

O principal objetivo do projeto é permitir a exploração das funcionalidades oferecidas pelo `ArrayList`. Ele serve como um exemplo prático para entender e testar o comportamento dessa estrutura de dados em relação à adição, pesquisa e remoção de elementos.

Ao executar o programa, você poderá interagir com o `ArrayList` e verificar como as operações de adicionar, pesquisar e remover são realizadas. Isso ajudará a entender como o `ArrayList` é eficiente para armazenar e manipular objetos do tipo `Aluno` de maneira dinâmica. Tudo isso é realizado através da interface gráfica do JOptionPane, que permite receber os dados do usuário de forma mais dinâmica.

## 📙Como Utilizar o Programa

1. Baixe o código-fonte do projeto e importe-o para a sua IDE Java preferida.
2. Execute o programa.
3. Siga as instruções fornecidas pelo programa para interagir com o `ArrayList` de alunos.
4. Experimente as diferentes operações, como adicionar um novo aluno, pesquisar alunos pelo nome e remover alunos pela matrícula.
5. Observe os resultados e familiarize-se com as funcionalidades e comportamentos do `ArrayList`.

## ✅Conclusão

O projeto de teste de funcionalidades do `ArrayList` com a classe `Aluno` e o `GerenciadorOperacoes` proporciona uma oportunidade de explorar e entender melhor a utilização dessa estrutura de dados. Ao experimentar diferentes operações no `ArrayList`, você poderá aprimorar seu conhecimento sobre a manipulação dinâmica de objetos e a funcionalidade do `ArrayList` no contexto de um projeto Java.
